export enum MediaFolder {
  Avatar = 'avatars',
}

export enum AvatarType {
  Team = 'team',
  User = 'user',
  Channel = 'channel',
}

export enum AvatarSize {
  XS = 24,
  SM = 32,
  MD = 48,
  LG = 72,
  XL = 256,
  XXL = 512,
  Original = 'original',
}
